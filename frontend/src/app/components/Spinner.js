const Spinner = () => {
    return (
        <div className="loadingSpinnerContainer">
            {/* El CSS se encarga de la animación circular */}
            <div className="loadingSpinner"></div>
        </div>
    );
}

export default Spinner;